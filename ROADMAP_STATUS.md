# PSLT Roadmap Status

Last updated: 2026-04-17

This file tracks roadmap items that are still open, partially promoted, or explicitly held behind release gates. It is intended as an operational companion to `/Users/boypatrick/codex/PSLT_quantam/README.md`, `/Users/boypatrick/codex/PSLT_quantam/FAMILY_DECISION_TABLE.md`, and `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex`.

For a one-page summary of which reviewer families are canonical, closed-negative, or still worth keeping as research ingredients, see `/Users/boypatrick/codex/PSLT_quantam/FAMILY_DECISION_TABLE.md`.

## Current Release Gate Snapshot

Sources:
- `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/chain_mode_cell_direct_audit_Dgrid21_Egrid41.csv`
- `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/chain_mode_cell_direct_audit_Dgrid60_Egrid21.csv`
- `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/chain_mode_cell_direct_audit_Dgrid21_Egrid41_cell_direct_runtime_release_tuned.csv`
- `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/chain_mode_cell_direct_audit_Dgrid60_Egrid21_cell_direct_runtime_release_tuned.csv`

| Branch | Gate | Acceptance mismatch | `max|Δμ_μμ|` | Release Threshold | Status |
| --- | --- | ---: | ---: | --- | --- |
| `cell_direct_runtime` | `D21xE41` | `0.000000` | `0.000000` | mismatch `<=0.01`, `max|Δμ|<=1.0` | PASS |
| `cell_direct_runtime` | `D60xE21` | `0.000000` | `0.000000` | mismatch `<=0.01`, `max|Δμ|<=1.0` | PASS |
| `cell_direct_runtime_release_tuned` | `D21xE41` | `0.000000` | `0.092481` | mismatch `<=0.01`, `max|Δμ|<=1.0` | PASS |
| `cell_direct_runtime_release_tuned` | `D60xE21` | `0.000000` | `0.837269` | mismatch `<=0.01`, `max|Δμ|<=1.0` | PASS |

Interpretation:
- `full_direct` remains the release baseline.
- `cell_direct_runtime` has now passed both release gates and is promoted as the release-production parity branch.
- `cell_direct_runtime_release_tuned` now also passes both gates with a D-only risk-weighted profile anchor `alpha(D)` rather than a fixed `0.99` blend.
- In physical terms, the promoted release-production parity branch closes the direct spectral-selection side of the chain (`g_N+\chi+A`) against `full_direct`, while the promoted `cell_direct_runtime_release_tuned` branch closes a **risk-weighted profile-anchored runtime-direct visibility parity path** in the observable sector.
- The current canonical release profile is `output/kinetic_action_chain/runtime_direct_visibility_alphaD_profile_release.csv`, selected by minimum mean anchor among gate-passing candidates (`alpha_min=0.96`, `alpha_max=0.99`, `power=1.0`, `mean(alpha)=0.9637`, `p90(alpha)=0.9734`).
- A post-promotion de-anchoring probe is now recorded in `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_visibility_alphaD_deanchoring_probe_summary.csv`. In the tested neighborhood, every lower-anchoring candidate re-opened the `D60xE21` gate through either acceptance mismatch (`alpha_min < 0.96`) or `max|Δμ_{\mu\mu}| > 1` (`alpha_max < 0.99`). The current canonical `alpha(D)` should therefore be treated as **close to the minimal necessary anchoring** for the present runtime-direct visibility implementation.
- The canonical reviewer-side model-chain branch is now `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compd60snapmaplate`. The older `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost` branch remains the historical refreshed comparator baseline used in reviewer evidence and hotspot decomposition.
- A `ref_D` snap bug in the first `twolobe` full-map validations was fixed on 2026-03-29. Corrected reviewer revalidation reopens raw `twolobe` as a genuinely positive parent runtime-descendant family rather than a catastrophic negative: `D60 p95 |Δμ_{\mu\mu}| 31.29 -> 18.58`, `D21 p95 2.10 -> 1.78`. The first repaired descendant, `twolobe_d8maplate`, preserves that `D60` gain exactly while reducing `D21 p95` further to `0.89` with zero acceptance flips, but a broader regression audit shows it still leaves the `D21` complement strips at `D=7.2/9.6` untouched. A newer descendant, `twolobe_d8compmaplate`, closes those complement strips at map layer while preserving the same `D60` gain: `D21 all-points p95 0.9137 -> 0.8786`, `D21 reviewer p95 = 0.8910`, `D21 complement p95 1.4133 -> 0.7570`, zero acceptance flips. Broad regression plus cross-grid holdouts on `D60×E41` and `D21×E21` keep that same structure intact, with no new material hidden regression beyond the same tiny D60 non-reviewer micro-drift near `D≈7.254, eta=4.0`. A fully snapped `D≈7.24` repair (`twolobe_d8compd724snapmaplate`) lowers that high-resolution D60 drift but reopens the coarse-grid `D21` complement because the snapped center lands back on `D=7.2`. The D60-only conditional version, `twolobe_d8compd60snapmaplate`, keeps `D21` identical to `twolobe_d8compmaplate` while improving the dense-grid D60 drift strip (`D60E21 1.2051 -> 1.2028`, `D80E41 1.7153 -> 1.6774`, `D100E41 1.0419 -> 0.9349`). Its explicit promotion checklist closes `20/20` gates, the final publish smoke/regression checklist closes `22/22` gates with `GO_branch_switch_recommended`, and a dedicated canonical post-switch monitoring audit now reproduces the same localized D60 drift-strip improvement while leaving reviewer targets unchanged and introducing no new reviewer-side acceptance flips. This line has therefore moved from pending monitoring setup to routine surveillance.

## Must-Have Before Stronger PRD Claim

| Item | Current State | Main Evidence | Next Gate / Requirement |
| --- | --- | --- | --- |
| Close EYMH projected parent-kernel derivation from parent action | Structural bridge closed and the late-stage UV narrative now reads as a reviewer-facing proposition chain rather than a draft audit stream. The canonical EYMH normalization is controlled by an exact projected `logdet + Schur` kernel, exact parent-kernel excess functional, exact block split, a background-normalized parent block-determinant / Schur-complement identity, a uniqueness audit showing that the mixed block is the geometric mean selected by the minimal symmetric-excess family, a ratio-warp audit showing that the first non-minimal extension is rejected on the D21xE21 fix grid, a symmetry/normalization audit showing that the same family is uniquely selected by zero normalization shift, unit symmetric degree, and zero antisymmetric tilt, a generator-affinity audit showing that the first local non-affine log-curvature corrections are also rejected, an excess-coordinate audit showing that the unique exact reference offsets are the identity participation block and the shell/background tree block themselves, a fixed-point Box-Cox audit showing that even among smooth tangent-normalized coordinate warps the unique exact point remains the linear additive excess choice, and a normal-coordinate audit showing that the first nonlinear local jets are uniquely rejected at zero second jet. `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` now packages this as `Proposition U1` (UV-to-EFT witness), `Proposition U2` (factorized EYMH prefactor), `Proposition U3` (projected response action), and `Proposition U4` (kernel-selection principle). The dense `D41xE21` target decomposition remains supportive rather than foundational: `E_part` behaves as a slow envelope, while `E_tree` is better described as a low/mid-`D` multi-frequency sector with a high-`D` cutoff, and a piecewise `E_tree` fit improves target reconstruction beyond the global smooth or single-regime alternatives. The bridge checklist still closes `10/10` structural gates, and there is no longer an obvious proof-gap blocker inside the local EYMH subsection. | `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_kernel_statement_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_block_split_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_blockdet_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_mix_geomean_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_ratio_warp_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_symnorm_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_parent_generator_affinity_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_excess_coordinate_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_fixedpoint_boxcox_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/hll_uv_action_loop_eymh_normal_coordinate_audit_summary_D21E21_fix.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/eymh_parent_kernel_bridge_status_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/eymh_parent_kernel_bridge_status_gates.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/eymh_parent_kernel_bridge_status_gaps.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_uv_matching/hll_uv_action_loop_eymh_parent_excess_summary_D41E21_targetdense.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_uv_matching/hll_uv_action_loop_eymh_parent_etree_regime_fit_summary_D41E21_targetdense.csv`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` | The next gate is no longer to repair an obvious local proof gap. Remaining work is optional strengthening rather than a mainline blocker: one may add a stronger appendix-level complement estimate, a more explicit local perturbation bridge between generator curvature and Schur corrections, or extra numerical/audit exposition, but the core EYMH subsection can now be treated as locally closed and reviewer-defensible. The dense-target regime split should remain reader-facing interpretation unless a stronger derivation-side statement can be made without circularity |
| Push risk-weighted `alpha(D)` anchoring closer to strict all-direct closure | Focus-band closure remains the historical motivation, but the broader-grid line is now best treated as a parked late-polish endpoint. After the original reopened grid-aware rule, exact case-aware retunes closed the dominant strips one mechanism at a time: `D21` now uses `D=4.0` width `\beta=1.22`, `D=4.8` width `\beta=0.694`, and `D=6.4` `g_{\rm norm}` band-pass `(g_\beta,g_{\rm center},\Delta g)=(0.31,0.07,0.045)`; `D60` uses `D=4.0` width `\beta=1.304`, `D=4.8` width `\beta=0.56`, `D=6.4` localized amplitude `0.048`, `D=7.2` width `\beta=0.455`, and case-aware `D=8.0` widths `(\beta_{D21},\beta_{D60})=(1.20,1.48)`. Under the current parked `v26` rule, the exact broader-grid validator keeps `acceptance_mismatch = 0.0 / 0.0`, `D21xE21_holdout p95|Δ\mu_{\mu\mu}| = 0.0560`, and `D60xE21_release p95|Δ\mu_{\mu\mu}| = 0.1097`. The final cheap `D21 D\approx4.8` probes now tell a cleaner story than the earlier `v25` stop point: a tight beta micro-scan lowers the slice again and identifies a narrow local optimum at `0.694`, but positive `D21 D48` local amplitude still over-corrects immediately and a `3\times3` `D48` center/half-width geometry probe still shows no nontrivial pocket away from the baseline `D=4.8` center. This item is therefore no longer an active optimization target; `v26` should be treated as the strict all-direct late-polish endpoint unless a genuinely new mechanism class is introduced. | `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_generalization_fast_summary_gridaware_rule_d40d64d48d64amp_v26.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_generalization_fast_detail_gridaware_rule_d40d64d48d64amp_v26.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_generalization_fast_decision_gridaware_rule_d40d64d48d64amp_v26.json`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_d21_d48_beta_probe_v5_v25_micro_summary.json`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_d21_d48_beta_probe_v6_v25_micro_refine_summary.json`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_d21_d48_local_amp_probe_v1_summary.json`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/runtime_direct_detlin_d21_d48_centerwidth_probe_v1_3x3_summary.json` | Treat `v26` as the parked strict all-direct late-polish endpoint. Reopen this line only for a genuinely new mechanism class that can target a residual `\eta`-slope structure directly; do not put `D21 D48` local-amplitude or band-geometry micro-sweeps back on the critical path, and only revisit `D48` beta if a broader simplification pass re-couples the existing case-aware knobs. |
| Full `(D, eta, N)` localized projection / model-chain unification | Canonical reviewer mainline now has a standing post-switch surveillance artifact rather than only a pre-switch promotion checklist. Release-production parity is exact for `g_N+\chi+\tilde A_\ell`, and the canonical reviewer observable branch is `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compd60snapmaplate`. The historical refreshed comparator `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost` still records the original reviewer gap (`D60\times E21` mismatch `0.042857`, `max|\Delta\mu_{\mu\mu}|=31.29`, `p95=8.96`; `D21\times E41` mismatch `0.090592`, `max|\Delta\mu_{\mu\mu}|=2.10`, `p95=0.914`), and the hotspot decomposition from that branch remains the structural baseline: a partial/point-amp band at `D≈6.169/6.441`, a width band at `D≈4.814/5.627/6.712`, and the `D=6.4` guard on D21. Cross-grid negatives (`pointamp3`, `widthband`, `partialguard`) stay closed, while the repaired `twolobe` line has already cleared the switch path after the 2026-03-29 `ref_D` snap fix: corrected raw `twolobe` landed at `D60 p95=18.58`, `D21 p95=1.78`; `twolobe_d8maplate` preserved that `D60` gain while reducing `D21 p95` to `0.89`; `twolobe_d8compmaplate` then closed the `D21` complement strips (`D21 all-points p95=0.8786`, reviewer `0.8910`, complement `0.7570`, zero acceptance flips); and the D60-only conditional descendant `twolobe_d8compd60snapmaplate` preserved that repaired `D21` structure while improving the dense-grid D60 drift strip from `1.2051 -> 1.2028` on `D60`, `1.7153 -> 1.6774` on `D80`, and `1.0419 -> 0.9349` on `D100`. The final publish smoke/regression checklist closes `22/22` gates and the new canonical monitoring audit reproduces the same localized drift-strip improvements while keeping reviewer targets and reviewer-side acceptance unchanged, so this line has advanced from pending monitoring setup to routine surveillance. | `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_D60E21_refresh_20260324.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_D21E41.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/model_chain_fullwidthrefamp_pointamp2_widthboost_d60_reviewerref_hotspot_audit_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_runtime_mode_validation_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8_maplate_currentbase_runtime_validation_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8comp_maplate_full_runtime_validation_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8comp_maplate_broad_regression_audit_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8comp_maplate_crossgrid_holdout_audit_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_runtime_validation_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_promotion_gate_checklist.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_promotion_gate_checklist_gates.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_publish_switch_checklist.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_publish_switch_checklist_gates.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_d60_monitoring_audit_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_d60_monitoring_audit_slices.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_d60_monitoring_audit_eta_profiles.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_D60E21_runtime_20260329_refsnapfix.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_D21E41_runtime_20260329_refsnapfix.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8maplate_D60E21_runtime_20260329_refsnapfix_d8maplate_r3.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8maplate_D21E41_runtime_20260329_refsnapfix_d8maplate_r3.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compmaplate_D60E21_runtime_20260330_refsnapfix_d8comp_r1.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_signal_strength/hll_signal_strength_summary_chain_mode_cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compmaplate_D21E41_runtime_20260330_refsnapfix_d8comp_r1.csv` | Treat `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compd60snapmaplate` as the canonical reviewer mainline under routine surveillance rather than as a pending promotion candidate. Keep `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost` as the historical comparator baseline, use `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_d60_monitoring_audit_summary.csv` as the standing post-switch health check, continue watching that the residual D60 non-reviewer micro-drift remains localized as grids widen, and keep `widthband`, `pointamp3`, `partialguard`, reviewer-scalar, and reviewer ref-handoff closed unless a substantially different mechanism class is introduced |

## Good-to-Have

| Item | Current State | Main Evidence | Next Gate / Requirement |
| --- | --- | --- | --- |
| Propagate `t_coh` first-principles candidate into baseline | Benchmark only | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:865`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1638` | Show map-level stability and replace control-parameter baseline if acceptable |
| Propagate `eta_fp(D)` candidate into baseline | Benchmark only | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:866`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1702` | Show stable impact and production-quality closure |
| Fully localized channel-resolved barrier-leakage normalization | Partial: action-derived `A_l(D)` profile baseline exists | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:148`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:867`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1777` | Replace profile-level insertion with full scan-grid localized extraction |

## Reviewer-Driven Follow-Ups

These are the most useful external-review-style upgrades to remember after the current release stabilization. They are not all equal in urgency: some are already reflected in the paper's current caveats, while others remain real proof-level or observable-level gaps.

| Item | Current State | Main Evidence | Next Gate / Requirement |
| --- | --- | --- | --- |
| Strict static resonance width extraction | The paper now explicitly states that the present static width law is a reviewer-facing surrogate rather than a completed first-principles derivation. The current baseline keeps `\Gamma_{N,\ell}^{(geo)}=\omega_N e^{-2S_{N,\ell}}` as the conservative static surrogate, while the minimal CAP pilot is recorded only as infrastructure because both densified scans remained absorber-dominated and did not show a clean plateau. The correct reviewer-facing direction is therefore "complex-pole / Siegert-style width extraction or a plateau-resolved non-Hermitian method," not a claim that the current CAP outputs are already physical widths. | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:481`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:3220`, `/Users/boypatrick/codex/PSLT_quantam/code/extract_cap_resonance_1d.py`, `/Users/boypatrick/codex/PSLT_quantam/output/cap_resonance_1d/` | Keep `\Gamma_{N,\ell}^{(geo)}` as the reported static surrogate until one of two things happens: either a non-Hermitian extraction shows a genuine plateau / stable complex pole, or a stronger outgoing-wave / Siegert / ECS / DtN derivation replaces the current template cleanly. |
| Mode-resolved high-`N` threshold theorem | The neutral-saturation wording is now fixed in the paper: `B_{N>3}=1` is not the mechanism that removes a fourth layer. A minimal audit already shows that the audited single-track benchmark never exhibits a fourth bound level, while the current 1D kinetic proxy keeps `E_4>0` across the tested window and does not support a special tiny-`\Gamma_4` hierarchy. This is strong reviewer-facing evidence for threshold / binding loss, but it is still not a theorem. | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:536`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1668`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_highN_decoupling_minimal.py`, `/Users/boypatrick/codex/PSLT_quantam/output/highN_decoupling/highN_decoupling_single_track_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/highN_decoupling/highN_decoupling_superrad_1d_summary.csv` | Upgrade the current evidence into a mode-resolved statement: either prove a continuum-threshold condition for `E_4(D)` on the audited parameter window, or derive the first nontrivial high-layer leakage bound strongly enough that "dominant three-generation sector" sharpens into a genuine no-fourth-layer theorem on a stated domain. |
| `H\to\mu\mu` absolute normalization from the EYMH parent side | The gap is now narrower than a generic reference-anchor complaint, and narrower than the previous three-scalar bridge wording suggested. The bridge audit rewrites the current UV+LL-RG observable exactly in terms of `(A_*, R_{e/\mu}^*, R_{\tau/\mu}^*)`. The follow-up source audits then separate those constants cleanly: `R_{e/\mu}^*` is already fixed by the accepted-region invariant audit for practical purposes, and `R_{\tau/\mu}^*` is an exact UV-tree invariant of the current diagonal chain (`max |ΔR_{\tau/\mu}^{\rm UV\to IR}| = 3.23e-14` on active rows; the fixed-`R_{e/\mu}^*` two-parameter bridge fit returns the same `R_{\tau/\mu}^*` up to `2.86e-07` relative drift). The remaining observable-side normalization problem is therefore primarily the absolute amplitude `A_*`, which itself factorizes exactly as `A_* = A_*^{(tree)} Z_{{\rm diag},*}^{-2}` with only a `1.11%` dressing shift between `A_*^{(tree)}` and `A_*`. The tree-source audit sharpened this to the single-layer source `C_{\mu\mu}^{\rm tree}=y_2^{raw}(D)\,[P_2^{kin}(D,\eta)/M_2^2(D)]`, the overlap-source audit identified `y_2^{raw}(D)=y_{\mu,2}^{flavor}(D)=(g_{\mu 2}^{UV}(D))^2` exactly because `sigma_scale_mu=1`, the kernel audit narrowed the unresolved block to the finite-box midplane bridge `K_\mu(\rho,z;D)=A_\mu^{disc}(D)\exp[-(\rho^2+z^2)/\sigma_\mu^2]`, the width audit split `\sigma_\mu` into exact and parent-side layers, the quartic-correction audit showed that `c_\sigma(D)=\sigma_\mu \bar\kappa_\mu(D)^{1/4}` is already almost D-invariant (relative span `1.38e-03`) and that the corrected candidate `\sigma_\mu^{(curv,4)}(D)=c_{\sigma,*}\bar\kappa_\mu(D)^{-1/4}[1+b_4(q_{4,\mu}(D)-q_{4,\mu}(D_*))]` with `b_4=0.4244` reduces the width drift to `8.47e-07` and the kernel defect to `9.47e-05`, essentially saturating the finite-box floor. The remaining bridge constant is now supported by two more overlap-side checks: a pointwise core-to-box inversion returns `c_\sigma^{box,local}(D)` on the same narrow band (excluding the known `D=5` near-merger tracking outlier, mean `22.3033`, min `22.2825`, max `22.3100`, relative span `1.23e-03`), and a projected-Yukawa benchmark at `D={6,12,18}` re-solves the fine overlap block with the original shift-invert target and finds exact canonical rebuild closure (`max relerr = 1.27e-13`) together with `c_\sigma^{projected}=22.3095`, only `1.60e-04` away from `c_\sigma^{ref}` and `2.24e-05` from `c_\sigma^{amp}`. A new analytic EYMH-core audit now identifies the body of that constant more directly: the isolated one-center Plummer/EYMH core already gives `\kappa_{self}=6353.3061` and hence `c_\sigma^{self}=22.3198`, within `1.78e-03` of the extracted bridge band, while the full local two-center jet tracks the finite-difference EYMH Hessian to `1.68e-04` relative error and yields `c_\sigma^{analytic}(D)` with only `4.21e-05` maximum relative error across D4--20. The residual mirror correction is itself small, `\delta\kappa_{mirror}/\kappa_{self}\in[-6.91e-03,-1.39e-03]`, so the current best reading is that `c_\sigma≈22.31` is set primarily by the one-center EYMH core, with the second center supplying only a mild D-dependent refinement. A further projected-box source audit now closes the loop more explicitly: feeding `c_\sigma^{self}` directly into the projected-Yukawa box-normalized overlap functional at `D={6,12,18}` reproduces the block with `y_2` log-RMSE `6.15e-03`, maximum relative error `9.26e-03`, and mean relative error `4.93e-03`, while the analytic mirror-corrected `c_\sigma^{analytic}(D)` improves the same benchmark to log-RMSE `6.29e-04` and maximum relative error `9.89e-04`, slightly outperforming the fixed projected selector itself. So the present best statement is no longer merely that the one-center core sets a nearby constant; rather, the one-center EYMH core already fixes the bulk of the projected-Yukawa box normalization, and the small two-center mirror correction is enough to saturate the projected benchmark. | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:777`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1664`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_absolute_normalization_bridge.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_absolute_amp_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_tau_ratio_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_tree_mumu_parent_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_y2raw_parent_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_kmu_parent_candidate.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_sigma_mu_parent_candidate.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_sigma_mu_curvature_correction.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_core_box_local.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_projected_overlap.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_eymh_core_source.py`, `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_projected_box_source.py`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_absolute_normalization_bridge_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_absolute_amp_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_tau_ratio_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_tree_mumu_parent_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_y2raw_parent_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_kmu_parent_candidate_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_sigma_mu_parent_candidate_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_sigma_mu_curvature_correction_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_csigma_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_csigma_core_box_local_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_csigma_projected_overlap_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_csigma_eymh_core_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/hll_csigma_projected_box_source_summary.csv` | Treat the observable-side bridge as mostly resolved on the flavor-ratio side. Keep `R_{e/\mu}^*` anchored to the invariant audit, retain `R_{\tau/\mu}^*` as an exact diagonal-chain UV invariant unless the chain changes, and push the remaining parent-side derivation pressure onto `A_*` itself. Concretely: derive the reference overlap block `y_2^{raw}(D_*)`, keep the kernel at the finite-box midplane bridge level, and treat the width profile as essentially structurally fixed. The next genuinely nontrivial target is now to explain the one-center EYMH core normalization that sets `c_\sigma`, with the mild two-center mirror correction already isolated; after that, the quartic-response coefficient `b_4` is the first curvature/Hessian correction worth deriving. |
| Equation-level theorem / surrogate labeling polish | This critique is only partially live now because the paper already includes a `Model-Chain Assumption Status` table, and the late-stage UV/open-system sections are already written as proposition/theorem-target chains. The remaining gain would be presentation-side rather than foundational. | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:136`, `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:1658` | If another final-structure pass is needed, propagate the current module-level status table into lighter equation-level or subsection-level tags (`derived`, `audited numeric`, `EFT surrogate`, `theorem target`) so that reviewers do not have to reconstruct the rigor split from context alone. |

Short submission-facing checklist:
- `must-fix`: derive the remaining `H\to\mu\mu` absolute amplitude `A_*` from the EYMH/UV side; within the current diagonal chain this is now most naturally a derivation of the reference overlap block `y_2^{raw}(D_*)`, with `R_{e/\mu}^*` already fixed by the invariant audit and `R_{\tau/\mu}^*` an exact UV-tree invariant.
- `must-fix`: keep the static-width language conservative until a genuine complex-pole / plateau-resolved extraction exists.
- `should-fix`: upgrade the current high-`N` threshold evidence into a mode-resolved statement, ideally an `E_4(D)` threshold theorem on a stated domain.
- `can-polish`: push the current assumption-status split down from the table level to lighter equation/subsection tags if another structure pass is made.
- `can-polish`: keep the spectrum/kinetics narrative aligned with the later audits by treating the hydrogenic `1-1/(2N^2)` law only as a legacy comparator, the Kerr-shaped `\hat\omega^{4\ell+5}` factor only as a residual hierarchy convention, and `r_N=\eta e^{-2S_N}` as a coarse-grained suppression factor rather than a literal tunneling probability.

## Post-Submission / Longer Horizon

| Item | Current State | Main Evidence | Next Gate / Requirement |
| --- | --- | --- | --- |
| Higher-dimensional parent-source derivation for projected Poisson source | Hook implemented, not integrated into baseline | `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex:145`, `/Users/boypatrick/codex/PSLT_quantam/README.md:172` | Provide a specific parent solution and connect it to baseline geometry |

## Completed Recently

These should not be re-listed as "not done":

- The front-of-paper spectral/kinetic wording was cleaned up to match the current audited state. In `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex`, the mainline spectral input is now the action-derived `\omega_N^{\rm(exact)}(D)` with the hydrogenic `1-1/(2N^2)` law demoted to a legacy comparator; the kinetic section now treats the Kerr-shaped `\hat\omega_N^{4\ell+5}` factor only as a residual hierarchy convention absorbed into the action-derived profile factors `\tilde A_\ell(D)`; and the `r_N=\eta e^{-2S_N}` paragraph now calls `r_N` an effective rank-2 suppression factor rather than a literal tunneling probability. This was a narrative cleanup only: no baseline maps or scan outputs changed.

- `full_direct_map` release mode exists and is published through `/Users/boypatrick/codex/PSLT_quantam/code/publish_full_direct_map.py`.
- Release-gate protocol exists and exports:
  - `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/full_direct_release_gate_status.csv`
  - `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/full_direct_worst_points_table.csv`
- Worst-point local refinement helper exists:
  - `/Users/boypatrick/codex/PSLT_quantam/code/run_localized_direct_refine_from_worst.py`
- `open_system_micro` multi-anchor + holdout calibration is implemented.
- A minimal CAP-based static-resonance pilot plus plateau-window / refinement heuristics now exist in `/Users/boypatrick/codex/PSLT_quantam/code/extract_cap_resonance_1d.py`, with the current benchmark artifacts recorded under `/Users/boypatrick/codex/PSLT_quantam/output/cap_resonance_1d/`.
- `cell_direct_runtime` now passes both release gates exactly (`D21xE41` and `D60xE21`) and is promoted as the release-production parity branch.
- Minimal Dirac-conformal Yukawa-overlap roadmap text is implemented in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex`.
- A minimal high-`N` decoupling audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_highN_decoupling_minimal.py`, with summaries exported to `/Users/boypatrick/codex/PSLT_quantam/output/highN_decoupling/`. On the audited single-track benchmark no tested `D` point supports a fourth bound layer, and the companion one-dimensional kinetic proxy keeps `E_4>0` across the tested window while a forced quasi-channel continuation does not generate an extra `\Gamma_4` hierarchy. The current reviewer-facing reading is therefore threshold / binding loss rather than an engineered visibility cutoff, but not yet a mode-resolved no-fourth-layer theorem.
- A new absolute-normalization bridge audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_absolute_normalization_bridge.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. On the canonical `action_loop_eymh_parented` D21xE21 map, the exact invariant rewrite closes to machine precision, a three-parameter global bridge fit stays within `max |Δ\mu_{\mu\mu}| = 1.12e-05`, and freezing `R_{e/\mu}^*` to the accepted-region invariant median still leaves machine-level closure. This reduced the observable-side problem from a generic scan-anchor choice to a compact bridge-constant question, which the follow-up source audits then narrow further.
- Follow-up observable-side source audits now exist in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_absolute_amp_source.py` and `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_tau_ratio_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. On active rows of the canonical `action_loop_eymh_parented` D21xE21 map, both diagonal flavor ratios are exact UV-tree invariants to machine precision (`max |ΔR_{e/\mu}^{\rm UV\to IR}| = 6.05e-13`, `max |ΔR_{\tau/\mu}^{\rm UV\to IR}| = 3.23e-14`), and at the live reference point `R_{\tau/\mu}^*` is identical across tree/match/IR. The only remaining observable-side normalization target is therefore primarily the absolute amplitude `A_*`, whose exact source audit shows `A_* = A_*^{(tree)} Z_{{\rm diag},*}^{-2}` with factorization residual `9.32e-17` and only a `1.11%` total diagonal dressing shift.
- A new overlap-source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_y2raw_parent_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. On the canonical overlap extractor, `sigma_scale_mu=1` identically, so `y_2^{raw}(D)=y_{\mu,2}^{flavor}(D)=(g_{\mu 2}^{UV}(D))^2` pointwise on the audited profile, with machine-level residuals below `1e-16`. The same audit shows that a center-mode-only source is not adequate (`mean` relative defect `8.61e-01`, `p95` `1.14` on the D4-20 detail grid), so the remaining `A_*` target should be treated as a microcanonical-windowed mu-flavor overlap block rather than as a single tracked-mode overlap.
- A new kernel-source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_kmu_parent_candidate.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. In the canonical baseline (`sigma_l=sigma_r=2.5`, `frame_power=0`) the muon overlap kernel itself factorizes exactly as a symmetric midplane bridge `K_\mu(\rho,z;D)=A_\mu^{disc}(D)\exp[-(\rho^2+z^2)/\sigma_\mu^2]`, verified with relative sup residual `3.05e-15` on the D4-20 overlap grid. Replacing the discrete normalization by the corresponding finite-box continuum amplitude leaves only a `~1e-4` kernel-level defect, while the naive infinite-volume amplitude is still off by `~2.37e-1`. This means the remaining normalization-side parent target is now more specific than "derive the kernel": the gross kernel shape is already fixed, and the next nontrivial source question is the origin of the effective width `\sigma_\mu` (or its first curvature-controlled correction) inside that finite-box midplane bridge block.
- A new width-source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_sigma_mu_parent_candidate.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It cleanly separates the remaining `\sigma_\mu` question into three layers: the exact kernel log-Hessian extraction reproduces `\sigma_\mu=2.5` to machine precision (`max` relative error `2.49e-15`), a finite-box second-moment inversion still returns `\sigma_\mu=2.4999` with only `4.09e-05` maximum relative error, and a reference-calibrated core-Hessian candidate built from the action-derived `U=V_{\rm eff}-m_0^2` stays within `1.01e-03` of the canonical width while keeping the induced kernel deformation at only `1.11e-02` in relative sup norm across D4-20. This means the remaining normalization-side parent target is narrower again: the gross width profile is already structurally fixed, so the next real source question is the single calibration constant `c_\sigma` or its first curvature/Hessian correction.
- A new bridge-constant / quartic-correction audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_sigma_mu_curvature_correction.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It shows that the reference-calibrated bridge constant `c_\sigma(D)=\sigma_\mu \bar\kappa_\mu(D)^{1/4}` is already almost D-invariant on the audited D4-20 window (relative span `1.38e-03`). Introducing the first isotropized quartic invariant `q_{4,\mu}(D)=\lambda_\mu(D)/\bar\kappa_\mu(D)^{3/2}` and fitting one global correction coefficient `b_4=0.4244` collapses the remaining width drift from `1.01e-03` to `8.47e-07`, while pushing the finite-box kernel defect down from `1.11e-02` to `9.47e-05`. This effectively saturates the box-normalization floor, so the next source target is now more specific again: derive the nearly constant bridge coefficient `c_\sigma`, and only then the first quartic-response coefficient `b_4`.
- A new `c_\sigma` source-selector audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It compares direct width matching, finite-box amplitude matching, and full-kernel matching for the same bridge constant, and all three selectors collapse onto the same narrow band: `c_\sigma^{ref}=22.3033`, `c_\sigma^{width}=22.3025`, `c_\sigma^{amp}=c_\sigma^{kernel}=22.3100`. The amplitude/kernel selectors coincide and lower the maximum finite-box amplitude/kernel mismatch from `1.02e-02` to `1.88e-03`, so the remaining source question is now narrower again: explain why this nearly unique finite-box bridge constant emerges from the EYMH / projected-Yukawa side.
- Two further overlap-side audits now support the same `c_\sigma≈22.31` bridge band. The pointwise core-to-box inversion in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_core_box_local.py` reconstructs `c_\sigma^{box,local}(D)` directly from the finite-box amplitude relation; excluding the known near-merger tracking outlier at `D=5`, the local constants have mean `22.3033`, min `22.2825`, max `22.3100`, and relative span `1.23e-03`. The projected-Yukawa benchmark in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_projected_overlap.py` then re-solves the fine overlap block at `D={6,12,18}` with the original shift-invert target and verifies exact canonical rebuild closure (`max relerr = 1.27e-13`), while the projected selector lands at `c_\sigma^{projected}=22.3095`, only `1.60e-04` from `c_\sigma^{ref}` and `2.24e-05` from `c_\sigma^{amp}`. This means the finite-box bridge constant is now supported not only by kernel-side selectors, but also by direct overlap reconstruction.
- A new analytic EYMH-core source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_eymh_core_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It expands the shifted potential `U=m_0^2(\Omega^2-1)+(1-6\xi)\nabla^2\Omega/\Omega` directly around one core. In the isolated one-center Plummer limit this already gives `\kappa_self=6353.3061` and hence `c_\sigma^{self}=22.3198`, within `1.78e-03` of the extracted bridge band. Including the second center analytically in the local two-center jet then tracks the finite-difference EYMH Hessian to `1.68e-04` relative error and yields `c_\sigma^{analytic}(D)` with maximum relative error `4.21e-05` across D4--20. The residual mirror correction remains small, `\delta\kappa_{mirror}/\kappa_{self}\in[-6.91e-03,-1.39e-03]`, so the bridge constant is now best read as an essentially one-center EYMH core source plus a mild two-center refinement.
- A new projected-box source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_projected_box_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It pushes the EYMH-core reading one step further by inserting `c_\sigma^{self}` and the mirror-corrected `c_\sigma^{analytic}(D)` directly into the same projected-Yukawa box-normalized overlap functional whose benchmark selector defines `c_\sigma^{projected}`. On `D={6,12,18}`, the one-center source alone sits only `4.61e-04` above `c_\sigma^{projected}` and already reproduces the overlap block with `y_2` log-RMSE `6.15e-03` and maximum relative error `9.26e-03`; the analytic mirror correction then improves the same benchmark to log-RMSE `6.29e-04` and maximum relative error `9.89e-04`, slightly better than the fixed projected selector itself. This is the strongest normalization-side statement so far: the one-center EYMH core already fixes the bulk of the projected-Yukawa box normalization, while the small two-center mirror correction is enough to saturate the projected benchmark.
- The paper now promotes that result to a formal bridge claim (`Proposition U5`) rather than leaving it as prose-only interpretation. The proposition states that the projected-Yukawa box-normalized overlap functional is fixed in bulk by the one-center EYMH / Plummer source and saturated, on the current representative benchmark, by the small analytic mirror correction. This makes the stopping point sharper: the next decision is no longer whether the normalization-side bridge exists, but whether to treat the proposition as final on the current audited domain or to keep tightening the mirror/quartic remainder.
- A new synthesis audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_absolute_normalization_synthesis_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It compresses the normalization-side chain into one reviewer-facing statement: the flavor-ratio side, the exact amplitude factorization `A_* = A_*^{(tree)} Z_{{\rm diag},*}^{-2}`, the single-layer tree reduction `C_{\mu\mu}^{tree}=y_2^{raw}(P_2^{kin}/M_2^2)`, the overlap identity `y_2^{raw}=y_{\mu,2}^{flavor}=(g_{\mu2}^{UV})^2`, and the Gaussian midplane kernel are all already closed to machine precision or exact structural accuracy on the audited domain. The same synthesis also shows that the width profile is structurally fixed: the curvature width law is already within `1.01e-03`, the quartic correction lowers that to `8.47e-07`, and the finite-box kernel defect saturates at `9.47e-05`. What remains is only secondary tightening in the bridge coefficient and its first corrections: excluding the known `D=5` merger outlier the pointwise bridge constant stays on the narrow band `[22.2825,22.3100]`, the one-center source `c_\sigma^{self}=22.3198` sits only `7.37e-04` above the reference selector, the analytic two-center jet tracks the extracted `c_\sigma(D)` to `4.21e-05`, and the projected-box benchmark is already saturated to `9.89e-04` maximum relative `y_2` error by that analytic mirror refinement. In the paper this is now packaged as `Proposition U6 / Corollary U6`: the mainline normalization chain is structurally closed, while only `c_\sigma`, the analytic mirror correction, and the quartic coefficient `b_4` remain as secondary tightening directions.
- A further tree-source audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_tree_mumu_parent_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. On the canonical parented baseline (`uv_blend=0`), the muon tree coefficient reduces exactly to `C_{\mu\mu}^{\rm tree}(D,\eta) = y_2^{raw}(D)\,[P_2^{kin}(D,\eta)/M_2^2(D)]`, with machine-level residual `9.76e-17` and exact `N=2` support on every active row. At the live reference point the two factors are `y_2^{raw}=1.65981e-04` and `P_2^{kin}/M_2^2 = 2.62559e-01`, and across active rows the variation of `C_{\mu\mu}^{\rm tree}` is dominated by the overlap factor rather than by the kinetic/mass coefficient. The most efficient next parent-side normalization target is therefore the reference overlap block `y_2^{raw}(D_*)`.

## Recommended Execution Order

Pause point (2026-04-16):
- `H\to\mu\mu` absolute-normalization bridge is parked after the `K_\mu`, `\sigma_\mu`, quartic-correction, and `c_\sigma` source-selector audits. The current stopping point is: flavor-ratio side mostly resolved; kernel shape structurally fixed; width profile essentially closed; and the remaining source ambiguity compressed to the EYMH origin of the nearly unique finite-box bridge coefficient `c_\sigma≈22.31`, with the first quartic-response coefficient `b_4` clearly secondary.
- `H\to\mu\mu` absolute-normalization bridge is parked after the `K_\mu`, `\sigma_\mu`, quartic-correction, `c_\sigma` source-selector, core-to-box inversion, projected-overlap, analytic EYMH-core, and projected-box source audits. The current stopping point is: flavor-ratio side mostly resolved; kernel shape structurally fixed; width profile essentially closed; and the remaining source ambiguity compressed to the one-center EYMH normalization that sets the nearly unique finite-box bridge coefficient `c_\sigma≈22.31`, with the small two-center mirror correction already shown to saturate the projected benchmark and the quartic-response coefficient `b_4` clearly secondary.
- With `Proposition U5` now in the paper, the current stopping point is even narrower: the mainline normalization statement has been elevated from an audit narrative to a proposition-level bridge claim, and what remains is mostly a judgment call about how much further to formalize the residual mirror / quartic correction hierarchy.
- With `Proposition U6 / Corollary U6` now added, the stopping point is cleaner again: the flavor-ratio side, amplitude factorization, single-layer tree source, overlap identity, kernel shape, and structural width law are all explicitly classified as closed blocks, while only `c_\sigma`, the analytic mirror correction, and the quartic coefficient `b_4` remain as secondary tightening.
- A new `c_\sigma` tightening synthesis now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_csigma_tightening_synthesis.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It shows that the remaining bridge coefficient is no longer best read as an unresolved selector band: the analytic EYMH coefficient satisfies the exact factorization `c_\sigma^{analytic}(D)=c_\sigma^{self}(1+\delta\kappa_{mirror}(D)/\kappa_{self})^{1/4}` with machine-level residual `3.55e-15`, the mirror fraction itself stays tiny (`[-6.91e-03,-1.39e-03]`), and the resulting mirror multiplier lies in the narrow interval `[0.998269,0.999653]`. Against the extracted D4--20 bridge coefficient, the one-center source alone stays within `1.78e-03`, while the analytic mirror-refined coefficient improves this to `4.21e-05`; on the projected-box benchmark the same refinement lowers the maximum relative `y_2` error from `9.26e-03` to `9.89e-04` and the log-RMSE by a factor `9.79`. In the paper this is now promoted to `Proposition U7 / Corollary U7`: the `c_\sigma` mainline is treated as closed by the one-center EYMH core constant plus the tiny analytic mirror multiplier, and only the quartic-response coefficient `b_4` together with sub-`10^{-3}` projected-box polish remain as secondary tightening.
- A new `b_4` tightening synthesis now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_hll_b4_tightening_synthesis.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/hll_absolute_normalization/`. It shows that after `U7` the quartic correction is no longer naturally read as a new width-side mechanism. On the fixed coordinate `q_{4,\mu}(D)-q_{4,\mu}(D_*)`, the entire residual correction is already exhausted by one global scalar `b_4=0.4244007048874373`: the maximum width error drops from `1.01e-03` to `8.47e-07`, the maximum finite-box kernel defect from `1.11e-02` to `9.47e-05`, and subset re-fits change `b_4` only by `6.27e-05` relative when excluding `D=5`, by `3.98e-06` on the outer window `D<=8 or D>=12`, and only at the permille level even under left/right half splits. In the paper this is now promoted to `Proposition U8 / Corollary U8`: after `U7`, the normalization-side quartic correction is already just one stable scalar response coefficient, so anything beyond it is benchmark polish rather than a new mainline closure problem.
- When resuming this line, the next most natural steps are:
  1. if more precision is needed, work only on sub-`10^{-3}` projected-box polish;
  2. otherwise, treat the normalization side as closed and move the next major effort back to the open-system theorem / high-`N` lines.

1. Treat `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost_twolobe_d8compd60snapmaplate` as the canonical reviewer mainline under routine surveillance. The pre-switch branch `cell_direct_runtime_release_fullwidthrefamp_pointamp2_widthboost` remains the historical comparator baseline for reviewer regressions, while the standing health check is `/Users/boypatrick/codex/PSLT_quantam/output/kinetic_action_chain/observable_two_lobe_d8compd60snapmaplate_d60_monitoring_audit_summary.csv`, which confirms that the residual D60 non-reviewer drift improvement remains localized and reviewer targets stay unchanged.
2. Promote `open_system_micro` back to the main conceptual next step, but now with the narrowest target yet. Reviewer surveillance is already routine, EYMH is locally closed, and strict all-direct is parked in a late-polish state. The local bridge is explicit on the audited knot set, the reviewer-facing proposition chain is now written down in both `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` and `/Users/boypatrick/codex/PSLT_quantam/README.md` as `Proposition O1/O2/O3 -> Theorem Target O1`, and the remaining microscopic gap has already been decomposed into three concrete origin questions: the scalar modulus `kappa_env`, the local `Phi_a` jet coefficients, and the single-pole gap-locked response family behind the two on-shell kernels. Among those, the cleanest next target is still the dominant-pole source itself: show that the projected EYMH Schur resolvent admits a first-mode-dominant truncation on the audited on-shell window, with leading pole scale `Omega1 = omega1` and a uniformly small higher-mode remainder there. In parallel, keep the static-width upgrade path alive but scoped: the current CAP pilot is still infrastructure rather than a physical-width result, and both densified CAP passes remain absorber-linear. The project-level conclusion is now to keep `Gamma_{N,\ell}^{(geo)} = \omega_N e^{-2S_{N,\ell}}` as the leading static surrogate, park CAP as infrastructure rather than a current blocker, and postpone any ECS / DtN escalation until the parent-side dominant-pole statement is clearer. The gap-conditioned audit means that the remaining theorem target is no longer an undifferentiated complement bound: it is enough to produce one parent-side lower bound on `g_a` together with one small on-shell defect estimate `delta_{q,a}`. The new `delta_q` source audit now effectively closes the defect half on the audited D4--20 knot set: the parent-bath anisotropy ratio, the direct micro ratio `S_{xx}(\Delta E)/S_{zz}(0)`, and the gap-locked Lorentzian prediction `1/(1+(\Delta E/\omega_1)^2)` agree to `10^{-16}` level, so the practical blocker has narrowed again to the gap / exact Schur-lift side alone. The new parent-side proxy audit pushes this slightly further still: a canonical complement-block proxy already gives a concrete nontrivial floor `g^{(proxy)}_a>=1.3537` on the active audited rows, so the next hard step is narrower than before. One no longer needs to guess whether a gap might exist; one needs to justify why the exact Schur-resolvent inherits or dominates this proxy gap. The latest operator-family audit strengthens even that wording: inside the canonical local family box, the exact parent objective already enforces the conservative floor `g_a>=1.3375` once `J_parent<=10^{-6}`, with admissible scan points still staying above `1.3537`. The conditional lifting audit then shows that even `sigma_a<=1e-2` would only relax that floor to `1.3308`. The new off-diagonal dominant-channel audit narrows the last missing ingredient again: in the minimal rank-one contraction model, the same local objective bound gives `|lambda|<=2.02e-1`, the exact overlap has `max xi_a = 1.3324e-1`, and the raw witness envelope `0.127472 * hk_abs_offdiag/diag + 0.435064 * action_abs_offdiag/diag` gives the conservative ceiling `xi_a <= 1.797e-1`, which still implies only `sigma_a <= 7.39e-3` at `J_parent<=1e-6`. The new true-operator consequence audit shows what that sigma budget would already mean for the full positive contraction: at the same tolerance, the exact canonical route gives `tr(A_a) <= 5.47e-3`, `||A_a|| <= 5.46e-3`, and Corollary-1 log-det remainder `<= 1.10e-2`, while the conservative raw envelope still gives only `tr(A_a) <= 7.39e-3`, `||A_a|| <= 7.36e-3`, and log-det remainder `<= 1.48e-2`. A cleaner 2x2 determinant-deficit reformulation now weakens that statement one step further: if the parent side can instead supply `u_a := 1-det(I-A_a)`, then one already gets `||A_a|| <= u_a` and `tr(A_a) <= 2*(1-sqrt(1-u_a))` without any single-channel assumption. A preliminary coarse audit already showed that this determinant-deficit route lives on the `10^{-3}` scale, but the densified local comparison is now the more useful benchmark. Resolving the same family on `alpha,beta in [0.95,1.05]` and `lambda in [-0.2,0.2]` with step `0.005` shows that the admissible ridge at `J_parent<=1e-6` actually runs near `(1.03,0.99,±0.2)`, giving the exact dense-family overlap budget `u_a <= 5.42e-3`. A continuum neighborhood optimization using the same local coercive budget is almost equally sharp, yielding `u_a <= 5.34e-3`, while the conservative raw-envelope fallback on the same dense admissible window stays at only `u_a <= 7.19e-3`. The latest consolidation audit narrows the lift step one notch further: across the explicit parent-bath family, generator-affinity, log-coordinate, and normal-coordinate scans, the exact statement is always selected at the undeformed point with canonical objective at most `3.18e-12`, while the weakest runner-up gap is already `6.90e-4`. That is `6.90e2` times the neighborhood tolerance `J_\ast=1e-6` and `2.17e8` times the largest canonical residual; the companion statement/cocycle/potential audits remain exact at `10^{-12}` to `10^{-15}`. A new transfer audit strengthens this from a gap statement to a uniqueness statement: at `J_\ast=10^{-6}`, each of the four explicit family scans has exactly one admissible point and zero non-canonical admissible points. So, conditional on the exact Schur-side operator staying inside any of those audited family classes, the Proposition-O5 budget transfers unchanged and yields `g_a^{exact} >= 1.3339`, `H_eff >= 0.9947 H_PP`, and, with the closed `delta_q` side, only `1-\nu_1 <= 5.12e-14`, `\varepsilon(\Delta E) <= 2.97e-14`. A new witness-space aggregation now weakens even that family-class hypothesis: stacking all four deformation scans directly in the operator residual coordinates `(block11, block22, trace, determinant, anisotropy)` shows that the nearest non-canonical challenger already sits at `||W_{95}||_\infty = 1.92e-4` and `||W_{max}||_\infty = 2.01e-4`, while the canonical witness suprema remain only `8.17e-13` and `8.43e-13`. So on the audited operator chart, the half-radius bounds `||W_{95}||_\infty <= 9.62e-5`, `||W_{max}||_\infty <= 1.01e-4` still leave margins of `1.18e8` and `1.19e8` over the canonical witness scale and transfer the same Proposition-O5 budget without naming a specific family class. One new direct chart-source audit pushes this almost to the end: reading the exported exact parent-bath statement map with the same p95/max conventions gives `||W_{95}^{(exact)}||_\infty = 3.52e-7` and `||W_{max}^{(exact)}||_\infty = 3.63e-7`, both still below the chart radii by factors `2.73e2` and `2.77e2`, while the cocycle/potential witnesses stay tiny (`1.11e-15`, `4.03e-15`). A final envelope audit now compresses even that chart-source statement back into the exact operator equations themselves: using only the factorization, statement, cocycle, and potential summary residuals gives the unified witness envelope `B_op^(exact) = 3.63e-7`, again dominated by the factorization-side block-22 term, and therefore `B_op^(exact) <= r_{95}^{safe}/2` and `B_op^(exact) <= r_{max}^{safe}/2` by factors `1.33e2` and `1.39e2`. One final reduction now isolates the dominant block-22 term itself: writing `Delta_mix^(fact) = |gamma_mix - kappa_env g_x^2 Sxx| / (g_x^2 Sxx)` and combining the exported numerator bound `eps_mix = 4.40e-14` with either the direct kernel floor `min g_x^2 Sxx = 1.2147e-9`, the equivalent gamma-floor `(min gamma_mix - eps_mix)/kappa_env = 1.2147e-9`, or the fully separated floor `(min g_x)^2 min Sxx = 1.1417e-9` yields conservative bounds `3.62e-5`, `3.62e-5`, and `3.85e-5`, all still below the same half-radii `4.81e-5`, `5.03e-5`. A further observable-chain reduction then removes `gamma_ref` as an independent blocker: the exported exact bridge obeys `gamma_ref = omega1^11 = tau_env^-11` to machine precision, so the audited reference-rate floor `475.002983...` is already exactly the first-mode floor `(omega1_floor)^11` (equivalently the gap-locked timescale ceiling `tau_env_ceiling^-11`). One more universal reduction removes `Cmax` as an independent blocker as well: positivity of any trace-one `2x2` density matrix implies `|rho_LR(t)| <= 1/2`, hence `Cmax <= 1/2` for the exact Lindblad-response block, and the exported bridge still leaves slack `0.5 - max Cmax = 2.461e-3`. Feeding this universal ceiling back through the observable chain still yields `gamma_mix >= 4.29102e-4`, `g_x^2 Sxx >= 9.96645e-10`, and the dominant-witness bound `||Delta_mix^(fact)||_inf <= 4.41e-5`, which remains below the half-radii by factors `1.09` and `1.14`. The newest floor-source audit now localizes the last scalar one step further: `chi_eff(D)` obeys the exact identity `chi_eff(D) = A_chi(D) Cmax(D)` with `A_chi(D) = 2 gamma_mix(D)/gamma_ref(D) = kappa_env * 2 g_x(D)^2 Sxx(D)/gamma_ref(D)`, and the same export gives a separated amplitude floor `A_chi^(sep floor) = 1.04147e-6` versus actual `A_chi^(exact floor) = 1.81567e-6`, a ratio `1.74`. The last scalar is now the coherence-response floor itself, and that line has now also been reduced to a closed-form underdamped statement: all audited knots satisfy `DeltaE > gamma_phi`, so `Cmax = [DeltaE/(2 sqrt(Gamma^2+omega^2))] exp[-(Gamma/omega) arctan(omega/Gamma)]` with `Gamma = gamma_phi + 2 gamma_mix` and `omega = sqrt(DeltaE^2-gamma_phi^2)`. A monotonicity audit on the exported parameter box gives `partial_DeltaE Cmax > 0`, `partial_gamma_phi Cmax < 0`, and `partial_gamma_mix Cmax < 0`, which in turn yields the separated floor `Cmax >= 1.44083e-1` from `(DeltaE_min, gamma_phi_max, gamma_mix_max)`. Combining this with `A_chi^(sep floor)` gives `chi_eff >= 1.50059e-7`, versus the actual exact floor `9.03367e-7`. A new ratio-box tightening now removes most of that slack: rewriting the same closed form as `Cmax = F(a,b)` with `a=gamma_phi/DeltaE`, `b=gamma_mix/DeltaE` shows that the response depends only on the dimensionless pair `(a,b)`, and the exported ratio box is monotone with `max dF/da = -2.44e-1 < 0`, `max dF/db = -4.88e-1 < 0`. The resulting ratio-box floor `F(a_max,b_max)=2.34328e-1`, minus the exported exact-vs-closed residual `2.82464e-4`, gives the rigorous exact-bridge floor `Cmax >= 2.34045e-1`, within `0.09%` of the actual minimum `2.34255e-1`. Feeding that into `A_chi^(sep floor)` improves the conservative observable floor to `chi_eff >= 2.43752e-7`. So the observable-chain side is now effectively closed on the current exact bridge: what remains is mainly amplitude-side slack, not the existence of a coherence-response floor. The only reason to reopen CAP soon would be if a future parent-side argument gives a sharper outgoing-channel target that materially changes the finite-box setup. A new amplitude-source audit now shows where the remaining slack sits: on the exported exact bridge one has `A_chi = 2 gamma_mix / omega1^11 = kappa_env * 2 g_x^2 Sxx / omega1^11` to `1e-16`, and the minima of `A_chi`, `gamma_mix`, and `g_x^2 Sxx / omega1^11` all coincide at `D=11`. Thus the exact amplitude floor is already aligned with the mix-rate floor, `A_chi^(exact floor) = 1.81567e-6`, and the residual factor `1.63858` comes entirely from replacing the local denominator `omega1(D_gamma,min)^11` by the global ceiling `omega1^ceil`. Combining this aligned amplitude floor with the tightened ratio-box `Cmax` floor yields `chi_eff >= 4.24949e-7`, leaving only a factor `2.126` to the actual exact floor `9.03367e-7`. So after `Propositions O15-O16`, the remaining observable-chain task is no longer a generic `A_chi` floor: it is specifically to turn the aligned `gamma_mix` floor into an a priori theorem-level statement.  A new mix-rate alignment audit sharpens that interpretation further: on the exported exact bridge `gamma_mix = kappa_env * g_x^2 * Sxx` to `4.40e-14`, and the minima of `gamma_mix`, `g_x`, `g_x^2`, and `g_x^2 Sxx` all occur at the same knot `D=11`. Thus the exact mix-rate floor is already aligned with the `x`-channel overlap floor, `gamma_mix^(exact floor) = 5.22995e-4`, while replacing the local response factor by the global response minimum only lowers it to `4.91560e-4`, a factor `1.06395`. So the remaining theorem gap is no longer naturally read as an `Sxx` floor problem: on the current exact bridge the response side is mild, and the next proof-side target is best treated as an a priori floor for `g_x`. A new parity-source audit now reduces even that overlap-side target: because the localized basis is the Hadamard rotation of the lowest parity pair, one has `g_x = |(V_loc)_{LR}| = |V11-V22|/2` and `g_z = |V12|` on the projected fluctuation matrix `deltaV`. The audit closes these identities to machine precision (`max |g_x-|V11-V22|/2| = 1.00e-16`, `max |g_z-|V12|| = 2.18e-18`) and shows that the minima of `g_x`, `|V11-V22|/2`, and `gamma_mix` all occur at `D=11`. So the remaining proof-side target is now best read as an a priori lower bound on the static parity-diagonal contrast `|V11-V22|`, not as a new bath/response theorem. A further one-center/mirror decomposition now writes the shifted potential as `U_2 = U_+ + U_- + U_x`, subtracts the same origin-centered spherical averages to obtain `deltaV = deltaV_+ + deltaV_- + deltaV_x`, and therefore decomposes the parity contrast exactly as `V11-V22 = I_+ + I_- + I_x`. This exact split closes at machine precision (`max ||U_2-U_+-U_--U_x|| = 8.88e-15`, `max ||deltaV-deltaV_+-deltaV_--deltaV_x|| = 7.11e-15`, reconstruction residual `3.79e-18`), while the one-center pair already carries `97.74%` to `99.833%` of the exact contrast across `D=4..20`; the mirror/cross remainder contributes only `0.167%` to `2.257%` and keeps the same sign as the exact contrast on every audited knot. At the exact floor knot `D=11`, `V11-V22 = 6.70795e-5`, the one-center pair contributes `6.69675e-5`, and the remainder is only `1.12022e-7`. So the remaining proof-side target is now narrower again: not a generic bath/response theorem, and no longer even a generic parity-contrast floor, but an a priori lower bound on the one-center pair contribution to `|V11-V22|` together with a coarse control of the small static mirror remainder. A new core-window floor audit now makes that last statement quantitative. Scanning symmetric windows `B_+(R) \cup B_-(R)` over `R in [1.20,1.40]` with `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_self_floor_source.py` identifies the optimal safe radius `R_*=1.35`, for which the core contribution always has the same sign as `I_{self,pair}` and the tail always has the opposite sign. The resulting uniform relation `|I_{self,pair}| >= lambda_* |I_{self,core}^{(R_*)}|` has `lambda_*=0.426269`, while the core-window floor itself is `1.33812e-4`, so one already gets `|I_{self,pair}| >= 5.70400e-5` on the exact bridge. This is only a factor `1.174` below the actual one-center-pair floor `6.69675e-5`, and the full parity-contrast floor remains only `1.176` above the same bound because the mirror remainder is both same-sign and tiny (`<= 2.257%`). The open-system proof target is therefore now best read as a theorem-level lower bound on the one-center core contribution itself, with only coarse signed remainder control left for `I_x`. A further compact-anchor audit now sharpens even that statement: inside the same optimal core window `R_*=1.35`, the box anchor `rho_#=0.70`, `z_#=1.21` already has the same sign as `I_{self,core}^{(R_*)}` on every audited knot, and the residual shell inside `R_*` keeps that sign too. This gives the direct exact-bridge bound `|I_{self,core}^{(R_*)}| >= |I_{self,anchor}^{(rho_#,z_#)}|`, with compact-anchor floor `1.32321e-4` versus exact core-window floor `1.33812e-4`, only a factor `1.01127` apart. Feeding this back through `lambda_*` leaves `|I_{self,pair}| >= 5.64043e-5`, so even the total parity-contrast floor is now only a factor `1.189` above a compact one-center anchor. The remaining proof-side task is therefore narrower still: prove a lower bound on that compact local one-center anchor, and keep both the rest of the core window and `I_x` only as same-sign / coarse signed completions.
 A new amplitude-source audit now shows where the remaining slack sits: on the exported exact bridge one has `A_chi = 2 gamma_mix / omega1^11 = kappa_env * 2 g_x^2 Sxx / omega1^11` to `1e-16`, and the minima of `A_chi`, `gamma_mix`, and `g_x^2 Sxx / omega1^11` all coincide at `D=11`. Thus the exact amplitude floor is already aligned with the mix-rate floor, `A_chi^(exact floor) = 1.81567e-6`, and the residual factor `1.63858` comes entirely from replacing the local denominator `omega1(D_gamma,min)^11` by the global ceiling `omega1^ceil`. Combining this aligned amplitude floor with the tightened ratio-box `Cmax` floor yields `chi_eff >= 4.24949e-7`, leaving only a factor `2.126` to the actual exact floor `9.03367e-7`. So after `Propositions O15-O16`, the remaining observable-chain task is no longer a generic `A_chi` floor: it is specifically to turn the aligned `gamma_mix` floor into an a priori theorem-level statement.  A new mix-rate alignment audit sharpens that interpretation further: on the exported exact bridge `gamma_mix = kappa_env * g_x^2 * Sxx` to `4.40e-14`, and the minima of `gamma_mix`, `g_x`, `g_x^2`, and `g_x^2 Sxx` all occur at the same knot `D=11`. Thus the exact mix-rate floor is already aligned with the `x`-channel overlap floor, `gamma_mix^(exact floor) = 5.22995e-4`, while replacing the local response factor by the global response minimum only lowers it to `4.91560e-4`, a factor `1.06395`. So the remaining theorem gap is no longer naturally read as an `Sxx` floor problem: on the current exact bridge the response side is mild, and the next proof-side target is best treated as an a priori floor for `g_x`. A new parity-source audit now reduces even that overlap-side target: because the localized basis is the Hadamard rotation of the lowest parity pair, one has `g_x = |(V_loc)_{LR}| = |V11-V22|/2` and `g_z = |V12|` on the projected fluctuation matrix `deltaV`. The audit closes these identities to machine precision (`max |g_x-|V11-V22|/2| = 1.00e-16`, `max |g_z-|V12|| = 2.18e-18`) and shows that the minima of `g_x`, `|V11-V22|/2`, and `gamma_mix` all occur at `D=11`. So the remaining proof-side target is now best read as an a priori lower bound on the static parity-diagonal contrast `|V11-V22|`, not as a new bath/response theorem. A further one-center/mirror decomposition now writes the shifted potential as `U_2 = U_+ + U_- + U_x`, subtracts the same origin-centered spherical averages to obtain `deltaV = deltaV_+ + deltaV_- + deltaV_x`, and therefore decomposes the parity contrast exactly as `V11-V22 = I_+ + I_- + I_x`. This exact split closes at machine precision (`max ||U_2-U_+-U_--U_x|| = 8.88e-15`, `max ||deltaV-deltaV_+-deltaV_--deltaV_x|| = 7.11e-15`, reconstruction residual `3.79e-18`), while the one-center pair already carries `97.74%` to `99.833%` of the exact contrast across `D=4..20`; the mirror/cross remainder contributes only `0.167%` to `2.257%` and keeps the same sign as the exact contrast on every audited knot. At the exact floor knot `D=11`, `V11-V22 = 6.70795e-5`, the one-center pair contributes `6.69675e-5`, and the remainder is only `1.12022e-7`. So the remaining proof-side target is now narrower again: not a generic bath/response theorem, and no longer even a generic parity-contrast floor, but an a priori lower bound on the one-center pair contribution to `|V11-V22|` together with a coarse control of the small static mirror remainder. A new core-window floor audit now makes that last statement quantitative. Scanning symmetric windows `B_+(R) \cup B_-(R)` over `R in [1.20,1.40]` with `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_self_floor_source.py` identifies the optimal safe radius `R_*=1.35`, for which the core contribution always has the same sign as `I_{self,pair}` and the tail always has the opposite sign. The resulting uniform relation `|I_{self,pair}| >= lambda_* |I_{self,core}^{(R_*)}|` has `lambda_*=0.426269`, while the core-window floor itself is `1.33812e-4`, so one already gets `|I_{self,pair}| >= 5.70400e-5` on the exact bridge. This is only a factor `1.174` below the actual one-center-pair floor `6.69675e-5`, and the full parity-contrast floor remains only `1.176` above the same bound because the mirror remainder is both same-sign and tiny (`<= 2.257%`). The open-system proof target is therefore now best read as a theorem-level lower bound on the one-center core contribution itself, with only coarse signed remainder control left for `I_x`. 3. Treat strict all-direct visibility as a parked late-polish endpoint rather than as an active blocker. The current `v26` case-aware rule keeps both broader-grid checks at zero acceptance mismatch and now lands at `D21xE21_holdout = 0.0560`, `D60xE21_release = 0.1097`. The remaining cheap `D21 D\approx4.8` band-level routes are now exhausted in a useful way: a narrow beta micro-scan located the local optimum at `0.694`, while positive local amplitude and center/width geometry both remained negative directions. Reopen this line only if a genuinely new mechanism class appears.
4. Treat EYMH projected parent-kernel derivation as locally closed on the present audit scope: the bridge audit closes the structural ambiguity, and `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` now carries a reviewer-facing `Proposition U1/U2/U3/U4` chain together with the underlying operator-level projector/Hessian and block-determinant / Schur evidence. The remaining work here is optional theorem strengthening or appendix exposition, not a blocker.
5. Keep the high-`N` physical narrative on the same footing as the current paper wording: `B_{N>3}=1` should continue to be treated as a neutral saturation rather than as the mechanism that removes a fourth layer. The new minimal audit supports a threshold / binding-loss interpretation more strongly than a tiny-`\Gamma_4` interpretation, so the next genuinely new step here would be a mode-resolved theorem-level audit of `E_4(D)` or of the first nontrivial high-layer leakage channel, not another visibility-tail retune.
6. Revisit `t_coh`, `eta_fp(D)`, and fully localized barrier-leakage normalization after the main proof-side / reviewer-side obligations are stronger.
7. Treat higher-dimensional source integration as the longer-horizon follow-up rather than the immediate PRD blocker.
- A new dominant-pole synthesis audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_dominant_pole_synthesis_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_dominant_pole_synthesis_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_dominant_pole_synthesis_source_detail.csv`, and the same reviewer-facing closure chain is now promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O40 / Corollary O39`. On the audited exact bridge, three blocks are now explicitly closed together: the on-shell defect side (`max |\delta_{q,a}| = 4.44e-16`, `max |\tau_{env,a}-\omega_{1,a}^{-1}| = 1.11e-16`), the family/chart transfer side (`N_adm^{(total)}=1`, `N_adm^{(noncan)}=0`, minimum selection-gap-over-tolerance `6.90e2` at `J_*=10^{-6}`), and the exact witness-envelope side (`B_op^{(exact)} = 3.63e-7`, still below the safe half-radii by factors `1.33e2` and `1.39e2`). The determinant-deficit neighborhood budget remains aligned across the continuum-neighborhood and exact-lift audits (`u_a <= 5.34e-3`, `g_a^{(exact)} >= 1.3339`, `H_eff >= 0.9947 H_PP`, with zero exported consistency residuals), so the remaining theorem target is now a single one: promote the same neighborhood budget from audited projected-family/chart evidence to a genuine parent-side analytic lift for the exact Schur resolvent. No new response family, support, or overlap object is still needed on this line.
- A new scalar Schur-lift reduction now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_lift_scalar_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_scalar_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_scalar_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O41 / Corollary O40`. It shows that the remaining exact Schur-lift gap is now genuinely one-dimensional: using `eps_mix = 4.40e-14` together with the tighter half-radius `r95/2 = 4.81e-5`, it is enough to prove the scalar mix-kernel floor `g_x(D)^2 S_{xx}(D) >= 9.15e-10` on the audited on-shell window (equivalently `gamma_mix(D) >= 3.94e-4`). The current exported bridge already exceeds that threshold by margins `1.328` (direct exact kernel floor), `1.328` (exact `gamma_mix` floor), and `1.095` (separated observable-chain floor), while the non-mix witness envelope is smaller than the same half-radius by the factor `2.06e5`. So the roadmap target should now be read as: no new chart/support/object remains on this line; the only missing theorem input is one parent-side analytic floor strong enough to support that scalar threshold.
- A new overlap-to-Schur bridge audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_lift_overlap_bridge.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_overlap_bridge_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_overlap_bridge_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O42 / Corollary O41`. It shows that even this one-dimensional Schur threshold is now very close to the existing static parity-contrast theorem chain: combining the compact-anchor exact-bridge floor `|V_{11}-V_{22}| >= 5.64043e-5` with the global response floor `S_{xx} >= 1.01493` already gives `gamma_mix >= 3.47553e-4`, i.e. `88.25%` of the scalar Schur threshold `3.93829e-4`. Equivalently, with the global response floor fixed, only a further `6.45%` tightening of the current static `g_x` floor would suffice; under the exact local-response alignment the remaining `g_x` tightening drops to `3.20%`. So the remaining dominant-pole roadmap item is now even narrower: not a new operator or response-family derivation, but a small constant-tightening step on the static parity-contrast floor.
- A new parity-threshold synthesis now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_lift_parity_threshold_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_parity_threshold_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_lift_parity_threshold_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O43 / Corollary O42`. It rewrites the scalar Schur-lift condition `g_x^2 S_{xx} >= 9.15e-10` as a single static parity threshold using `g_x = |V_{11}-V_{22}|/2`. Under the globally valid response floor the required theorem target is `|V_{11}-V_{22}| >= 6.00420e-5`; under the exact local-response alignment at the common floor knot it sharpens to `5.82096e-5`. The current theorem floors `5.64043e-5` and `5.70400e-5` already reach `93.94%/95.00%` of the global threshold and `96.90%/97.99%` of the local one, while the exact bridge floor `6.70795e-5` exceeds both thresholds by factors `1.117` and `1.152`. So the open-system micro roadmap should now be read in its sharpest current form: no new Schur structure remains at all, only an explicit small tightening of the already-isolated static parity theorem.
- A new exact-total core tightening audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_total_core_tightening_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_total_core_tightening_source_summary.csv`, `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_total_core_tightening_source_detail.csv`, and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_total_core_tightening_source_scan.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O44 / Corollary O43`. It keeps exactly the same symmetric core-window family `B_core(R)` and rescans it against the full exact parity contrast rather than the one-center pair alone. The same optimal radius `R_*=1.35` remains safe, but now yields the sharper theorem floor `|V_{11}-V_{22}| >= 5.74795e-5`, improving the previous `5.70400e-5` by the factor `1.00771` without changing support family. This already reaches `95.73%` of the globally valid Schur-parity threshold `6.00420e-5` and `98.75%` of the locally aligned threshold `5.82096e-5`, so the remaining dominant-pole theorem gap has narrowed again to a final `1.27%` static-parity tightening on the same overlap-side spine.
- A new exact-total window-split audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_total_core_window_split_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_total_core_window_split_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_total_core_window_split_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O45 / Corollary O44`. It does not change the support family at all: it keeps the same exact-total split, the same radius `R_*=1.35`, and only reads the same safe core/tail decomposition on the two contiguous audited subwindows `D=4..11` and `D=12..20`. The best cut is `D=11`, where the left induced floor is `6.46291e-5` and the right induced floor is `1.39072e-4`, so the same family already gives the audited piecewise floor `|V_{11}-V_{22}| >= 6.46291e-5` across the whole bridge. This exceeds both the global Schur-parity threshold `6.00420e-5` and the locally aligned threshold `5.82096e-5` by factors `1.07640` and `1.11028`. Therefore `open_system_micro` should now be read as audited piecewise closed on the current bridge: the remaining work is a parent-side analytic lift of the same exact-total split, not any new family, chart, or support design. The previous residual `1.27%` gap was only the loss from forcing one global `lambda_tot,*` and one global core floor to control the entire audited window at once.
- A new piecewise parent-lift audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_piecewise_parent_lift_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_piecewise_parent_lift_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_piecewise_parent_lift_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O46 / Corollary O45`. It turns the audited same-family closure into an explicit parent-side theorem target without introducing any new object: keep the same exact-total split and the same `R_*=1.35`, and prove on `D=4..11` the bounds `|J_tail| <= 0.520215 |J_core|`, `|J_core| >= 1.34704e-4`, while on `D=12..20` prove `|J_tail| <= 0.573291 |J_core|`, `|J_core| >= 3.25919e-4`. These same constants already reproduce the audited piecewise floor `|V_{11}-V_{22}| >= 6.46291e-5`; the left window is the only bottleneck, while the right window exceeds the global Schur-parity threshold by the factor `2.31625`. So the roadmap endpoint is now as narrow as it can reasonably be made: no new family design remains, only a parent-side analytic proof of the same piecewise core/tail inequalities.
- A new left-window bottleneck audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_left_window_bottleneck_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_bottleneck_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_bottleneck_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O47 / Corollary O46`. It formally demotes the right window to a non-bottleneck sector and shows that the remaining parent-side task on the same family `B_core(R_*=1.35)` can be expressed in two equivalent ways on the left window `D=4..11`: either prove a tail-ratio bound `eta_L <= 0.554268` (global threshold) or `eta_L <= 0.567871` (local threshold) while keeping the current left core floor, or prove a core-floor bound `C_L >= 1.25144e-4` (global) or `C_L >= 1.21325e-4` (local) while keeping the current left tail ratio. The audited values `eta_L = 0.520215` and `C_L = 1.34704e-4` already satisfy both forms, so the roadmap target is now completely localized: one left-window parent-side bottleneck theorem on the same exact-total split, with no new family, support, or chart design left to invent.
- A new left-window split audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_left_window_split_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_split_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_split_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O48 / Corollary O47`. It keeps the same family `B_core(R_*=1.35)` but shows that even the left-window bottleneck is mostly a coupling artifact: the tail-ratio peak occurs at `D=9`, while the core-floor minimum occurs at `D=11`. Splitting `D=4..11` at `D=10` therefore raises the same-family audited floor to `6.70795e-5`, which exceeds the global and local Schur-parity thresholds by factors `1.11721` and `1.15238`. So the roadmap endpoint narrows once more: the right window is demoted, the left-window coupling is broken, and the only practical parent-side task left is a same-family tail/core theorem on `D=4..10`, with `D=11` isolated as the core-floor knot.
- A new hierarchical left-window split audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_exact_schur_left_window_hierarchical_split_source.py`, with outputs `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_hierarchical_split_source_summary.csv` and `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/chi_open_system_exact_schur_left_window_hierarchical_split_source_detail.csv`, and is promoted in `/Users/boypatrick/codex/PSLT_quantam/paper/main.tex` as `Proposition O49 / Corollary O48`. It still keeps exactly the same family `B_core(R_*=1.35)`, but refines `D=4..10` into `D=4..6` and `D=7..10`. The best secondary cut is `D=6`, giving same-family floors `8.18812e-5` on `D=4..6` and `3.70831e-4` on `D=7..10`, while the already isolated singleton knot `D=11` remains at `6.70795e-5`. So every interval piece is now formally non-bottleneck on the audited bridge, and the only surviving audited minimum is the single core-floor knot `D=11`. This is the sharpest current reading of `open_system_micro`: the remaining parent-side task is no longer an interval family theorem, but a single-knot analytic lift on the same exact-total split.
- A new static-anchor reduction audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_anchor_static_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. On the canonical compact anchor `(\rho_\#,z_\#)=(0.70,1.21)`, the exact overlap-side source already splits as `I_{self,anchor}=I_{diag,anchor}+I_{mir,anchor}` with the same-center static term `I_{diag,anchor}` keeping the sign of the full anchor on every audited knot and the mirror correction staying uniformly bounded by `\mu_{anc}=0.415143` relative to that static term. This yields `|I_{self,anchor}| >= (1-\mu_{anc})|I_{diag,anchor}| >= 9.57744e-5`, only a factor `1.38159` below the exact anchor floor `1.32321e-4`. A scan over all safe same-center sub-boxes inside the anchor shows that the optimum already sits at the full canonical anchor itself, so the remaining proof-side target has narrowed again: prove an a priori lower bound on the same-center static anchor source `I_{diag,anchor}`, while the local mirror term stays only a coarse signed correction.
- A new strict same-center subbox audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_box_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It scans all genuine subboxes inside the canonical same-center anchor and shows that no smaller static patch improves the audited floor. The best strict plateau lies at `rho_box in [0.63,0.68]`, `zeta_box = 1.21`; a rounded representative box `(0.65,1.21)` still has the same sign as `I_{diag,anchor}` together with a same-sign shell on every audited knot, and its floor `1.48301e-4` is only a factor `1.10422` below the exact same-center-anchor floor `1.63757e-4`. So the proof-side target has stabilized again: the right theorem object is now the full same-center static anchor source `I_{diag,anchor}`, not a smaller localized subbox, while `I_{mir,anchor}` and `I_x` remain only coarse signed corrections.
- A new oriented-moment audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_moment_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It no longer changes the support; instead it rewrites the full same-center anchor itself as `|I_{diag,anchor}| = P_{diag} - N_{diag}` for the oriented density `q_{diag}` and shows that the exact split closes to `6.51e-19` on the audited window. The uniform cancellation ratio stays below `nu_diag^(±)=0.911532`, while the positive-moment floor is `min P_diag = 1.17067e-3`. This yields the direct intrinsic lower bound `|I_{diag,anchor}| >= 1.03567e-4`, stronger than the mass-only route by a factor `1.26719` and stronger than the induced compact-anchor bound by a factor `1.08136`. So the overlap-side theorem target has narrowed again: the remaining gap is now cancellation control inside the stabilized same-center anchor, not any further support selection.
- A new shell-isolated cancellation audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_cancellation_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It decomposes the same-center anchor moments into an inner axial slab and an outer shell, scanning `zeta_cut in [0.80,1.20]`, and finds the optimal nontrivial shell-isolating cut `zeta_cut^* = 1.20`. At that cut, `max N_out/P_in = 8.68193e-3` while `max N_in/P_in = 0.912743`, so the full audited cancellation ratio obeys the explicit bound `nu_diag^(±) <= 0.921425` and the shell contribution is below one percent of the normalized cancellation budget. The same audit also shows that this interior cancellation is mass-dominated rather than derivative-dominated: `min P_mass/P_diag = 1.00507`, `max P_der/P_diag = 8.53082e-2`, `min N_mass/N_diag = 0.712802`, and `max N_der/N_diag = 0.570882`. So the overlap-side theorem target has narrowed again: the remaining gap is now an interior one-center cancellation problem on the stabilized same-center anchor, not a support-selection or shell-leakage problem.
- A new interior mass-sector cancellation audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_cancellation_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It keeps the same optimal inner slab `zeta_cut^* = 1.20` and rewrites the stabilized same-center anchor floor as `I_{mass,in} = (1-\nu_{diag,int}^{(±,m)}) P_{mass,in}`. On the audited window, the interior mass term keeps the sign of the full anchor on every knot, and the exact anchor floor, the interior mass-net floor, and the maximal mass-sector cancellation ratio all align at the same knot `D=11`: `min |I_{diag,anchor}| = 1.65556e-4`, `min I_{mass,in} = 7.92440e-5`, `max nu_{diag,int}^{(±,m)} = 0.957812`. At that same floor knot, the derivative and shell pieces are already non-adverse (`I_{der,in}(11) = 8.38290e-5 > 0`, `I_{out}(11) = 2.48275e-6 > 0`). So the overlap-side target narrows again: the adverse floor-setting mechanism is the interior one-center mass-sector cancellation itself, while derivative and shell terms are now best treated as signed local corrections.
- A new positive-carrier audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_positive_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `P_{mass,in}` directly via monotonicity of the positive part on same-center subboxes. Scanning `rho_box in [0.35,0.70]` and `zeta_box in [0.70,1.20]` shows that the optimal strict positive-carrier plateau already lies on a near-full axial trim, `rho_box in [0.69,0.70]`, `zeta_box in [1.10,1.19]`. Using the canonical strict representative `(0.70,1.19)`, the audited floor is unchanged: `min P_{mass,box}^{(0.70,1.19)} = min P_{mass,in} = 1.25520e-3` at `D=20`, while the minimum all-knot share stays at `0.999825` (worst at `D=11`). So the positive-carrier side is now essentially closed, and the remaining overlap-side gap has narrowed once more to the interior mass-sector cancellation ratio itself.
- A new adverse-carrier annulus audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_negative_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `N_{mass,in}` directly by decomposing the negative mass carrier into an off-axis annulus `A_{neg}^{(rho_min,zeta_box)}` and an axial-core leakage term. Scanning `rho_min in [0.03,0.30]` and `zeta_box in [1.10,1.20]` shows that if arbitrary strict trims are allowed the optimum is a near-full slab, but once we insist on genuinely off-axis annuli `rho_min >= 0.12`, the best plateau sits at `rho_min in [0.12,0.15]`, `zeta_box = 1.20`. Using the canonical representative `(rho_min,zeta_box)=(0.15,1.20)`, the annulus already captures at least `94.9763%` of `N_{mass,in}` on every audited knot, while the omitted axial-core leakage satisfies `max N_{tail}/P_{mass,in} = 4.81178e-2` at `D=11`. The same floor knot also maximizes the annular adverse load, with `max N_{ann}/P_{mass,in} = 0.909694`. So the remaining overlap-side gap is no longer a full-slab negative-carrier problem; it has narrowed to an off-axis one-center adverse annulus plus a small axial-core leakage.
- A new strict adverse-annulus trim audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_annulus_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It scans all strict trims inside the canonical adverse annulus `A_{neg}^{(0.15,1.20)}` over `rho_min in [0.15,0.30]` and `zeta_box in [1.10,1.20]`. The best strict plateau already sits at `(rho_min,zeta_box)=(0.15,1.19)`: this trimmed annulus still captures at least `98.4061%` of the canonical adverse carrier (worst at `D=16`) and introduces only `max N_{cap}/P_{mass,in} = 7.47726e-3`. Its audited floor `7.64028e-4` is only a factor `1.01226` below the full-annulus floor `7.73394e-4`. So the support side of the adverse carrier is now effectively closed as well: the theorem target has stabilized at the full canonical adverse annulus, with only a tiny cap leakage left as a corrective constant.
- A new adverse-shoulder magnitude audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_shoulder_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks the remaining adverse magnitude directly by splitting the canonical annulus into a radial shoulder `N_{mass,shoulder}^{(\rho_{sh},1.20)}` and an inner-rim leakage `N_{mass,rim}^{(\rho_{sh},1.20)}`. Scanning strict radial trims `rho_sh in [0.16,0.30]` finds a flat optimal plateau `rho_sh in [0.16,0.20]`; using the canonical representative `rho_sh = 0.20`, the shoulder still captures at least `94.0824%` of the full adverse annulus (worst at `D=11`) while the discarded inner rim obeys `max N_{rim}/P_{mass,in} = 5.38319e-2` at `D=11`. The shoulder floor `7.37447e-4` is only a factor `1.04874` below the full-annulus floor `7.73394e-4`. So the proof-side target has narrowed again: the remaining overlap-side gap is now best read as an off-axis one-center adverse shoulder plus two small leakages (the inner rim and the axial core), rather than as a full-annulus magnitude problem.
- A new adverse-shoulder band audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_shoulder_band_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks the shoulder magnitude itself by imposing a genuine axial hollow inside the canonical shoulder `N_{mass,shoulder}^{(0.20,1.20)}`. Scanning `zeta_low in [0.10,0.40]` in the split `N_{mass,shoulder}=N_{mass,band}^{(0.20;\zeta_{low},1.20)}+N_{mass,core}^{(0.20;\zeta_{low},1.20)}` shows that the best hollow already sits at `zeta_low = 0.10`. This canonical adverse band still captures at least `95.1019%` of the full shoulder (worst at `D=11`), while the discarded shoulder-core leakage satisfies `max N_{core}/P_{mass,in} = 4.19214e-2` at `D=11`. Its audited floor exactly matches the shoulder floor, both equal to `7.37447e-4` at `D=19`. So the proof-side target has narrowed once more: the remaining overlap-side gap is now best read as a genuinely hollow off-axis adverse band plus three small leakages (axial shoulder core, inner rim, and axial core).
- A new adverse-band corner-stability audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_band_corner_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It tests whether the canonical adverse band `N_{mass,band}^{(0.20;0.10,1.20)}` can be improved by a genuinely strict corner trim. Scanning `rho_corner in [0.22,0.30]` and `zeta_corner in [0.10,0.40]` finds a flat best plateau `rho_corner in [0.22,0.27]`, `zeta_corner = 0.10`; using the canonical strict representative `(0.22,0.10)`, the corner subband still captures at least `92.1342%` of the full band (worst at `D=4`) while the discarded cap leakage reaches `max N_{cap}/P_{mass,in} = 6.35145e-2` at `D=11`. More importantly, the strict-corner floor drops to `6.88668e-4`, whereas the full adverse-band floor remains `7.37447e-4`, a loss by the factor `1.07083`. So support-side localization is now exhausted at the full adverse-band scale: the stable proof target remains the full canonical adverse band, with only small cap, shoulder-core, inner-rim, and axial-core leakages outside it.
- A new adverse-band magnitude factorization audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_mass_band_magnitude_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks the canonical adverse band `N_{mass,band}^{(0.20;0.10,1.20)}` directly, without any further support shrinkage, by splitting it into a positive one-center local mass kernel `K_{band}^{mass}` and a negative carrier density `W_{band}^{(-)}=[-(sgn I_{diag,anchor})\,\mathcal C]_+`. The audit verifies the exact pointwise factorization `[q_{mass}]_- = K_{band}^{mass} W_{band}^{(-)}` with zero residual on every audited knot, and shows that the kernel stays strictly positive on the whole canonical band, with audited minimum `1.82852e-2` at `D=4`. Writing `N_{mass,band} = \bar K_{band}^{(-)} W_{band}^{(-)}`, the weighted kernel average is trapped in the narrow interval `[5.38460e-2, 1.00728e-1]`, while the negative-carrier floor is `8.40086e-3` at `D=20`. The coarse product floor `K_{min}^{band} W_{band}^{(-)}` is `4.28523e-4`, and the exact adverse-band floor `7.37447e-4` exceeds it by the factor `1.72090`. So the support problem is now definitively closed, and the remaining overlap-side magnitude gap has narrowed again: the proof target is the negative carrier moment `W_{band}^{(-)}`, with the positive one-center mass kernel reduced to a bounded audited multiplier.
- A new negative-carrier radial-factor audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_band_negative_carrier_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `W_{band}^{(-)}` itself, still without shrinking support, by stripping off the radial Jacobian in `\mathcal C = 2\pi\rho(\psi_1^2-\psi_2^2)`. This yields an unweighted negative overlap-band moment `M_{band}^{(-)}` with the exact factorization `W_{band}^{(-)} = 2\pi \bar\rho_{band}^{(-)} M_{band}^{(-)}`. The audit verifies this with residual `1.35525e-20` and shows that the weighted radial factor is almost constant across the whole audited window, lying in `[0.4434097, 0.4438634]` with relative spread only `1.02267e-3`. Both the negative-carrier floor and the overlap-band floor occur at `D=20`, namely `8.40086e-3` and `3.01270e-3`, and even the coarsest lower product is essentially exact, with ratio `1.00088`. So the remaining carrier-side magnitude gap narrows once more: the proof target is no longer `W_{band}^{(-)}` itself, but the unweighted negative overlap-band moment `M_{band}^{(-)}`, with only an almost constant radial factor left outside it.
- A new overlap-band sign-balance audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_band_overlap_moment_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `M_{band}^{(-)}` directly, without any support change, by splitting it into an absolute overlap-band mass `M_{band}^{abs}` and a signed overlap residue `R_{band}^{(sgn)}`. The audit verifies the exact identity `M_{band}^{(-)} = (M_{band}^{abs} - R_{band}^{(sgn)})/2 = theta_{band}^{(-)} M_{band}^{abs}` with residual `1.73e-18`, and also verifies the localized-overlap rewrite `M_{band}^{abs} = 2 \int |\phi_R \phi_L|` with residual `3.47e-18`. The signed-imbalance ratio stays in `[-2.03788e-1, 9.60339e-2]`, so the negative-fraction factor obeys the uniform floor `theta_{band}^{(-)} >= 4.51983e-1`. Since the absolute overlap-band floor is `6.45299e-3` at `D=19`, this already induces `M_{band}^{(-)} >= 2.91664e-3`, only a factor `1.03294` below the exact negative-overlap floor `3.01270e-3` at `D=20`. So the support, kernel, and radial-weight questions are now effectively closed; the remaining overlap-side proof target has narrowed again to the absolute localized overlap-band mass `M_{band}^{abs} = 2 \int |\phi_R \phi_L|`, with only a small signed-imbalance factor left outside it.
- A new absolute localized-overlap audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_band_absolute_overlap_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `M_{band}^{abs}` directly on the same canonical band, with no further support shrinkage, by defining the same-center carrier amplitude `Phi_self` and mirror-tail amplitude `Phi_mir`. The audit verifies the exact factorization `M_{band}^{abs} = 2 \int \Phi_{self}\Phi_{mir} = 2 S_{band}^{(self)} \bar\Phi_{band}^{(mir|self)}` with zero residual. The same-center carrier moment has floor `1.66599e-1` at `D=20`, while the weighted mirror-tail average stays in `[1.92259e-2, 5.20575e-2]` and attains its minimum at `D=19`. Their direct product already gives `M_{band}^{abs} >= 6.40606e-3`, only a factor `1.00733` below the exact absolute-overlap floor `6.45299e-3`. The reverse route through the mirror carrier is weaker (`1.04982`). So the overlap-side proof target narrows once more: the absolute overlap-band mass is now essentially fixed by a one-center positive carrier times a weighted mirror-tail average, and the only remaining nontrivial multiplier is `\bar\Phi_{band}^{(mir|self)}`.
- A new weighted mirror-tail audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_band_mirror_average_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `\bar\Phi_{band}^{(mir|self)}` directly on the same canonical band, with no support change, by splitting it into the ordinary mirror-band mean `\bar\Phi_{band}^{(mir)} = S_{band}^{(mir)} / A_{band}` and a self-weight bias factor `\Xi_{band}^{(self)}`. The factorization `\bar\Phi_{band}^{(mir|self)} = \Xi_{band}^{(self)} \bar\Phi_{band}^{(mir)}` is exact with zero residual. The ordinary mirror-band mean has floor `2.43229e-2` at `D=20`, while the bias stays in the positive interval `[7.40007e-1, 8.35352e-1]`, with minimum at `D=5`. This already induces `\bar\Phi_{band}^{(mir|self)} >= 1.79991e-2`, only a factor `1.06816` below the exact weighted-mirror floor `1.92259e-2` at `D=19`. Meanwhile the canonical band area varies only between `2.3814` and `2.3976`, with relative spread `6.79e-3`, so the same statement is almost equivalent to a statement about the mirror carrier `S_{band}^{(mir)}`. So the multiplier-side proof target narrows again: the remaining object is now the ordinary mirror-band mean (or equivalently the mirror carrier, up to tiny geometric aliasing), while the self-weighting itself has been compressed into a bounded positive bias factor.
- A new ordinary mirror-band mean audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_diag_anchor_band_mirror_ratio_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It attacks `\bar\Phi_{band}^{(mir)}` directly, still with no support change, by factoring it through the same-center band mean and the mirror/self carrier ratio:
  \[
  \bar\Phi_{band}^{(mir)}
  =
  \left(\frac{S_{band}^{(mir)}}{S_{band}^{(self)}}\right)\bar\Phi_{band}^{(self)}.
  \]
  The residual is `6.94e-18`, and the structure is stronger than expected: the floor of `\bar\Phi_{band}^{(mir)}`, the floor of `\bar\Phi_{band}^{(self)}`, and the floor of `S_{band}^{(mir)}/S_{band}^{(self)}` all align at the same knot `D=20`. Numerically,
  \[
  \min \bar\Phi_{band}^{(mir)} = 2.43229\times10^{-2},\quad
  \min \bar\Phi_{band}^{(self)} = 6.99586\times10^{-2},\quad
  \min \frac{S_{band}^{(mir)}}{S_{band}^{(self)}} = 3.47676\times10^{-1},
  \]
  and their product reproduces the exact mirror-band mean floor to machine precision. So the ordinary mirror-band mean is now effectively closed as well: it is already saturated by a same-center positive carrier and a mirror/self ratio, and no further support-side refinement is needed on this line.
- A new synthesis audit now exists in `/Users/boypatrick/codex/PSLT_quantam/code/audit_open_system_parity_contrast_synthesis_source.py`, with outputs under `/Users/boypatrick/codex/PSLT_quantam/output/chi_open_system/`. It compresses `O19` through `O38` into one exact composite statement:
  \[
  N_{\rm mass,band}
  =
  4\pi\,\bar K_{\rm band}^{(-)}\bar\rho_{\rm band}^{(-)}\theta_{\rm band}^{(-)}
  \Xi_{\rm band}^{(self)}\Lambda_{\rm band}^{(mir|self)}
  \frac{S_{\rm band}^{(self)\,2}}{A_{\rm band}},
  \]
  together with
  \[
  \nu_{\rm diag,int}^{(\pm,m)}
  =
  \frac{N_{\rm mass,band}}{P_{\rm mass,in}}
  +
  \frac{N_{\rm mass,core}}{P_{\rm mass,in}}
  +
  \frac{N_{\rm mass,rim}}{P_{\rm mass,in}}
  +
  \frac{N_{\rm mass,tail}}{P_{\rm mass,in}}.
  \]
  The exact residuals are only `9.67e-17` and `5.97e-14`, respectively. At the worst cancellation knot `D=11`, the full interior ratio is reconstructed exactly as `0.957812 = 0.813941 + 0.041921 + 0.053832 + 0.048118`. This is the cleanest current stopping point: the support chain and ordinary mirror-band mean line are closed on the exact bridge, while only multiplier/leakage constants remain as secondary tightening directions.
